.container { padding:20rpx; }
.title { font-weight:bold; font-size:30rpx; margin-top:20rpx; }
.subtitle { font-weight:bold; font-size:28rpx; margin:20rpx 0 10rpx; }
input { border:1px solid #ccc; margin:10rpx 0; padding:10rpx; width:100%; border-radius:6rpx; }
button.primary { background-color:#07c160; color:#fff; border-radius:8rpx; margin:10rpx 0; padding:20rpx; font-size:28rpx; border:none; }
.checkbox-list { display:flex; flex-wrap:wrap; }

.card-grid { display:flex; flex-wrap:wrap; justify-content:flex-start; margin-top:10rpx; }
/* .card-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;    /* ← 中心对齐 */
  /* 如果多张时左右两端也贴边，可改成 space-between */
/* } */
/* .card-list.single .card {
  width: 90%;    /* 单张时铺满 90% */
  /* margin: 5% 0;  /* 居中 */
/* } */
/* .card-list.single {
  justify-content: center;
} */
.card {
  width: 28%;
  margin: 1.5%;
  /* margin: 1% 1%;             保持等分三列 */
  background: #f0f0f0;
  border-radius: 8rpx;
  overflow: hidden;
  text-align: center;
  padding-bottom: 10rpx;
}
.card-img { width:100%; height:150rpx; }
.card-title { font-size:24rpx; padding:8rpx; }
