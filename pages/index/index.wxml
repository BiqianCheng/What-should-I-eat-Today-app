<!--index.wxml-->
<!-- <scroll-view class="scrollarea" scroll-y type="list">
  <view class="container"> -->
    <!-- 用户登录界面和授权 -->
    <!-- <view class="userinfo">
      <block wx:if="{{canIUseNicknameComp && !hasUserInfo}}">
        <button class="avatar-wrapper" open-type="chooseAvatar" bind:chooseavatar="onChooseAvatar">
          <image class="avatar" src="{{userInfo.avatarUrl}}"></image>
        </button>
        <view class="nickname-wrapper">
          <text class="nickname-label">昵称</text>
          <input type="nickname" class="nickname-input" placeholder="请输入昵称" bind:change="onInputChange" />
        </view>
      </block>
      <block wx:elif="{{!hasUserInfo}}">
        <button wx:if="{{canIUseGetUserProfile}}" bindtap="getUserProfile"> 获取头像昵称 </button>
        <view wx:else> 请使用2.10.4及以上版本基础库 </view>
      </block>
      <block wx:else>
        <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
        <text class="userinfo-nickname">{{userInfo.nickName}}</text>
      </block>
    </view>
    <view class="usermotto">
      <text class="user-motto">{{motto}}</text>
    </view> -->
    <!-- <button class="primary" bindtap="recommendRecipes">推荐菜谱</button>
    <button class="primary" bindtap="goToRecipes">进入菜谱页面</button>
    <button class="secondary" bindtap="goToFridge">管理冰箱食材</button>
    </view> -->

<scroll-view class="scrollarea" scroll-y>
  <view class="container">
    <!-- 引导图片 -->
    <image src="/assets/今天做点什么吃.png" class="guide-image" />

    <!-- 说明文字：贴近“管理冰箱食材”按钮 -->
    <text class="intro-text intro-top">想看看你能用冰箱里的菜做些什么吃吗？</text>

    <!-- 按钮组：管理冰箱食材、推荐菜谱、进入菜谱页面 -->
    <view class="button-group">
      <button class="button full" bindtap="goToFridge">管理冰箱食材</button>
    </view>

    <!-- 说明文字：贴近“进入菜谱页面”按钮 -->
    <text class="intro-text intro-mid">有平时刷到的好吃的做饭教程吗？或者只是为了后续看看能做什么菜？把食谱需要的食材先放进来：</text>

    <view class="button-group">
      <button class="button full enter-recipes-btn" bindtap="goToRecipes">进入菜谱页面</button>
    </view>
  </view>
</scroll-view>

<!-- 页脚版权
<app-footer /> -->